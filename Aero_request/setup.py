from setuptools import setup

setup(
    name='Aerorequest',
    version='0.0.1',
    packages=find_packages(),
    install_requires=[
        'gym',
        'numpy',
        'pygame'
    ],
    
)
